# @flanksource/plugin-ui-sdk

Vendored browser SDK for calling Mission Control plugin operations.

This package is checked into `clicky-ui` temporarily so `@flanksource/clicky-ui`
can import `@flanksource/plugin-ui-sdk` before the SDK is published publicly.
Replace the local `file:` dependency with a registry version once available.
