{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "check": "tsc -p tsconfig.json --noEmit && oxlint tests playwright.config.ts --deny-warnings"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "typescript": "catalog:"
  }
}
