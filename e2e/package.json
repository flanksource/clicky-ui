{
  "name": "e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "lint": "oxlint tests playwright.config.ts --deny-warnings",
    "check": "tsc -p tsconfig.json --noEmit && pnpm run lint"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "typescript": "catalog:"
  }
}
