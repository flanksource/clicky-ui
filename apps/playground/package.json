{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @flanksource/clicky-ui build:icons && vite dev",
    "build": "pnpm --filter @flanksource/clicky-ui build:icons && tsc -b --noEmit && vite build",
    "preview": "vite preview",
    "lint": "oxlint src plugins vite.config.ts --deny-warnings",
    "check": "pnpm --filter @flanksource/clicky-ui build:icons && tsc -b --noEmit && pnpm run lint",
    "test": "vitest run"
  },
  "dependencies": {
    "@flanksource/clicky-ui": "workspace:*",
    "monaco-editor": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@testing-library/react": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "autoprefixer": "catalog:",
    "jsdom": "catalog:",
    "postcss": "catalog:",
    "react-grab": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
