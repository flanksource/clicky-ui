{
  "name": "storybook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @flanksource/clicky-ui build:icons && storybook dev -p 5270 --host 0.0.0.0",
    "build": "pnpm --filter @flanksource/clicky-ui build:icons && pnpm --filter @flanksource/clicky-ui build:storybook-schemas && storybook build",
    "serve": "pnpm --filter @flanksource/clicky-ui build:icons && pnpm --filter @flanksource/clicky-ui build:storybook-schemas && storybook build && vite preview --host 0.0.0.0 --port 5270 --strictPort --outDir storybook-static",
    "test": "pnpm --filter @flanksource/clicky-ui build:icons && pnpm --filter @flanksource/clicky-ui build:storybook-schemas && vitest run",
    "lint": "oxlint .storybook --deny-warnings",
    "check": "tsc -b --noEmit && pnpm run lint"
  },
  "dependencies": {
    "@flanksource/clicky-ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@chromatic-com/storybook": "catalog:",
    "@storybook/addon-docs": "catalog:",
    "@storybook/addon-vitest": "catalog:",
    "@storybook/react": "catalog:",
    "@storybook/react-vite": "catalog:",
    "@tailwindcss/vite": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitest/browser": "3.2.6",
    "@vitest/coverage-v8": "3.2.6",
    "playwright": "^1.61.0",
    "storybook": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
