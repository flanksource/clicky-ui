{
  "name": "kitchen-sink",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @flanksource/clicky-ui build:icons && vite dev",
    "build": "pnpm --filter @flanksource/clicky-ui build:icons && tsc -b --noEmit && vite build",
    "preview": "vite preview",
    "check": "pnpm --filter @flanksource/clicky-ui build:icons && tsc -b --noEmit && oxlint src vite.config.ts --deny-warnings"
  },
  "dependencies": {
    "@flanksource/clicky-ui": "workspace:*",
    "@shikijs/langs": "catalog:",
    "@shikijs/themes": "catalog:",
    "@shikijs/transformers": "catalog:",
    "@tanstack/react-query": "catalog:",
    "preact": "catalog:",
    "shiki": "catalog:"
  },
  "devDependencies": {
    "@preact/preset-vite": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "autoprefixer": "catalog:",
    "postcss": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
